#pragma once

#include <assert.h>
#include <stdlib.h>
#include <stdio.h>
#include <unistd.h>

struct IntegralInfo_t;

struct ThreadInfo_t;

//static void* GetThreadsInfo(size_t numThread, size_t* sizeThreadInfo);
void test();